# Force static provider since no runtime detected
providers = ["static"]

# Optional: Custom static assets path
[staticAssets]
path = "/"  # Serves index.html from root

# Or for Node.js if package.json exists:
# providers = ["node"]
# [phases.build]
# cmds = ["npm ci", "npm run build"]
# [start]
# cmd = "npm start"
